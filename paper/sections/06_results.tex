This section should consolidate the retained numerical outputs into a compact,
readable summary.

Suggested contents:

\begin{itemize}[leftmargin=1.5em]
\item table of phase verdicts and retained checks
\item key scalar outputs: purity, entropy, KD, memory proxy
\item one short sensitivity table if the QSOT2 line retains the alpha sweep
\item one compact paragraph on what remained numerically identical to the
  QSOT-Harness r1 math engine
\end{itemize}

The goal is to let a reviewer see, in one place, what the verifier actually
computed without having to reconstruct it from phase-by-phase prose.
